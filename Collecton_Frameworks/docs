-->used to store and process collection/group of dynamic data/objects.

        1>List : Dynamic Array
                a>ArrayList : slow processing (uses stack : push and pull from same end); better to use for data storage
                b>LinkedList: fast processing : better to use for data processing
                c>Vector

        2>Set  : collection of unique data
                a>HashSet : unique data only
                b>TreeSet : unique+sorted data
                c>LinkedHashSet : unique + insertion in order

        3>Map  : data managed in key value pair
                a>HashMap : key value pair/ <k,v> pair
                b>TreeMap : <k,v> + sorted data
                c>LinkedHashMap : <k,v> + insertion in order




       * list,set and map are interfaces and subheadings are classes to implement the interfaces



Java generics : runtime ma problem aauney kura laai solve garney class casting garera





exercise
1>Student set
2>Dog set





MAP:
Map<String,Integer> mpa =  new HashMap<>();